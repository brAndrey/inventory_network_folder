# folder_monitor

Система мониторинга структуры сетевых/локальных папок организации. Каждый час
сканирует заданные корневые папки, сохраняет структуру и количество файлов в
SQLite, сравнивает с предыдущим сканом и формирует отчёты об изменениях:
новые, удалённые, перемещённые папки и папки с аномальным уменьшением
количества файлов.

Только стандартная библиотека Python 3.8+, без внешних зависимостей.

## Ключевая особенность производительности

В папках очень много файлов, поэтому система **не измеряет размеры файлов** и
**не вызывает `stat()`** для файлов. Единственная метрика содержимого папки —
количество файлов непосредственно в ней (без вложенных папок). Для подсчёта
используется только `os.scandir()` и `entry.is_file(follow_symlinks=False)`,
что работает на кэшированных метаданных системного листинга каталога.

## Файлы проекта

| Файл | Назначение |
|------|-----------|
| `main.py` | Точка входа, оркестрация полного цикла. |
| `config_loader.py` | Чтение `config.ini`, разбор `путь;глубина`. |
| `database.py` | `DatabaseManager` и репозитории (scans/folders/events/errors/moves). |
| `scanner.py` | Обход папок и подсчёт файлов без `stat()`. |
| `diff_engine.py` | Сравнение сканов, детекция перемещений и аномалий. |
| `report_generator.py` | Отчёты CSV и HTML. |
| `email_sender.py` | Отправка e-mail через SMTP (с вложениями). |
| `send_report.py` | Принудительная отправка последнего отчёта. |
| `cleanup.py` | Удаление старых сканов, VACUUM. |
| `models.py` | Dataclasses для передачи данных. |
| `logger_setup.py` | Логирование с ротацией. |
| `config.ini` | Конфигурация (пример). |
| `run_monitor.bat` | Запуск для Планировщика задач Windows. |
| `run_send_report.bat` | Принудительная отправка последнего отчёта по e-mail. |
| `tests/` | Тесты (pytest). |

## Конфигурация (`config.ini`)

### Секция `[ROOTS]`

Каждая строка содержит адрес папки и индивидуальную глубину сканирования,
разделённые `;`:

```ini
[ROOTS]
root1 = \\fileserver\documents;3
root2 = \\fileserver\archive;1
root3 = D:\Docs\Проекты;5
root4 = \\fileserver\hr;2
```

Глубина:
- `0` — только сама корневая папка;
- `1` — корень + папки первого уровня;
- `N` — корень + N уровней;
- `-1` — без ограничения;
- если глубина не указана (строка без `;`) — считается `-1`.

### Секция `[OPTIONS]`

| Опция | По умолчанию | Описание |
|-------|-------------|----------|
| `db_path` | `folder_monitor.db` | Файл SQLite (относительно папки скрипта). |
| `log_dir` | `Log` | Папка логов. |
| `report_dir` | `Reports` | Папка отчётов. |
| `retention_days` | `7` | Хранение сканов (дни). |
| `file_count_decrease_percent` | `20` | Порог уменьшения файлов, %. |
| `file_count_decrease_min` | `10` | Порог уменьшения файлов, абсолют. |
| `scan_timeout_minutes` | `55` | Максимальное время сканирования. |
| `count_hidden_files` | `true` | Считать ли скрытые/служебные файлы. |

Аномалия уменьшения файлов срабатывает только при одновременном выполнении
обоих порогов (процент И абсолют).

### Секция `[ALERTS]` (опционально)

Позволяет отправлять оповещения по e-mail. Письмо отправляется **только** при
значимых изменениях:

- папка перемещена;
- папка удалена;
- аномальное уменьшение количества файлов в папке.

Если изменений нет — письмо не отправляется.

```ini
[ALERTS]
enabled = false
email_to = admin@example.com
email_from = monitor@example.com
smtp_server = smtp.example.com
smtp_port = 587
smtp_user =
smtp_password =
```

| Опция | По умолчанию | Описание |
|-------|-------------|----------|
| `enabled` | `false` | Включить оповещения по e-mail. |
| `email_to` | — | Адрес получателя. |
| `email_from` | `email_to` | Адрес отправителя. |
| `smtp_server` | — | SMTP-сервер. |
| `smtp_port` | `587` | Порт SMTP (используется STARTTLS). |
| `smtp_user` | — | Логин для SMTP-авторизации (пусто — без авторизации). |
| `smtp_password` | — | Пароль (см. ниже про хранение). |

Письмо содержит список перемещённых/удалённых папок и папок с аномальным
уменьшением файлов. Полные отчёты при этом сохраняются в папке `Reports/`.

### Хранение пароля SMTP

Если SMTP-сервер требует авторизацию, пароль можно задать двумя способами:

1. **Переменная окружения (рекомендуется).** Задайте `MONITOR_SMTP_PASSWORD`
   (и при необходимости `MONITOR_SMTP_USER`) в системе или прямо в задаче
   Планировщика. Переменная окружения имеет приоритет над `config.ini`, и
   пароль не хранится в файле открытым текстом.
2. **Опция `smtp_password` в `config.ini`.** Просто, но пароль лежит в файле
   открытым текстом — подходит только если к папке скрипта нет постороннего
   доступа.

Логин задаётся через `smtp_user` (или переменную `MONITOR_SMTP_USER`). Если
`smtp_user` пуст — авторизация не выполняется. Пароль нигде не логируется.

## Схема базы данных

Основные таблицы: `scans` (запуски), `folders` (структура папок скана),
`events` (значимые события), `scan_errors` (ошибки), `moves` (перемещения).

- Соединение открывается один раз на всё время работы, режим WAL.
- `PRAGMA foreign_keys=ON` обеспечивает каскадное удаление связанных записей
  при очистке старых сканов.

## Детекция перемещений

Папка считается перемещённой при сопоставлении «исчезнувших» и «появившихся»
папок внутри одного корня. Уровни уверенности:

- **high** — совпадают имя, `content_hash` и `file_count`;
- **medium** — совпадают имя и `content_hash` (число файлов изменилось);
- **low** — совпадает только имя, число файлов отличается не более чем на 10%,
  и такого имени нет среди неизменённых папок корня.

`content_hash` — SHA-256 от сортированного списка **имён** файлов (без размеров),
поэтому перемещение детектируется быстро даже при большом количестве файлов.

## Запуск

Вручную или через Планировщик задач Windows:

```bat
run_monitor.bat
```

Либо напрямую:

```bat
python main.py
```

Можно указать другой INI первым аргументом:

```bat
python main.py my_config.ini
```

## Принудительная отправка отчёта

Чтобы отправить последний сформированный отчёт по e-mail вручную (вне
расписания мониторинга), запустите:

```bat
run_send_report.bat
```

Либо напрямую:

```bat
python send_report.py
```

Скрипт находит самый свежий HTML-отчёт в папке `Reports/` и отправляет его
(вместе с одноимёнными CSV-отчётами) на адрес `email_to` из секции `[ALERTS]`.
Отправка происходит **принудительно** — независимо от того, были ли изменения
в последнем скане. Используются те же настройки SMTP, что и для оповещений.

### Коды завершения `send_report.py`

| Код | Значение |
|-----|----------|
| 0 | Отчёт отправлен |
| 1 | Ошибка конфигурации (нет INI, `[ALERTS]` выключены, не задан `email_to`/`smtp_server`) |
| 2 | В папке `Reports/` не найден ни один отчёт |
| 3 | Ошибка отправки по SMTP |

## Отчёты

После каждого сканирования в папке `Reports/` создаются:

- `report_YYYYMMDD_HHMMSS.csv` — полный отчёт об изменениях;
- `report_YYYYMMDD_HHMMSS.html` — человекочитаемый отчёт (перемещения с цветовой
  кодировкой: high — зелёный, medium — жёлтый, low — красный);
- `moves_YYYYMMDD_HHMMSS.csv` — только перемещения папок.

Формат CSV перемещений:

```
detected_at;root;old_path;new_path;folder_name;confidence;file_count
```

## Логирование

- Файл: `Log/monitor.log`, ротация 5 файлов по 5 МБ.
- Формат: `2026-09-09 14:00:01 [INFO] Сообщение`.
- Значимые события дублируются в таблицу `events`.
- Результат отправки/не отправки отчёта по e-mail (включая причину) фиксируется
  в логе при каждом обычном запуске мониторинга и при принудительной отправке.

## Тесты

```bash
pip install pytest
python -m pytest
```

Тесты используют `tempfile` и in-memory SQLite (`:memory:`).

## Коды завершения

| Код | Значение |
|-----|----------|
| 0 | Успех |
| 1 | Ошибка конфигурации |
| 2 | Ошибка БД |
| 3 | Таймаут сканирования |
| 4 | Критическая ошибка сканирования |
